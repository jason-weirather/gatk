#!/usr/bin/env bash
BASEDIR=$(dirname "$0")
if [[ $1 =~ ^- ]];
  then OPTION=$1;
  else OPTION='-Xmx4g';
fi
if [[ $1 =~ ^- ]]; 
  then java $OPTION -jar $BASEDIR/../jar/gatk.jar "${@:2}";
  else java $OPTION -jar $BASEDIR/../jar/gatk.jar "$@";
fi
